imports:
  - { resource: parameters.php }
  - { resource: ../../../../../vendor/knplabs/doctrine-behaviors/config/orm-services.yml }

framework:
    secret:          "Secret for tests!"
    router:          { resource: "%kernel.root_dir%/config/routing.yml" }
    session:         ~
    default_locale:  en
    translator:      { fallback: en }
    profiler:        { only_exceptions: false }
    templating:
        engines:
            - "twig"

parameters:
    database_driver: pdo_mysql
    database_host: %mantle.host% 
    database_port: null
    database_name: %mantle.db_name%
    database_user: %mantle.user% 
    database_password: %mantle.password% 

twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%

doctrine:
    dbal:
        driver:   pdo_mysql
        dbname:   %mantle.db_name% 
        user:     %mantle.user% 
        password: %mantle.password% 
    orm:
        default_entity_manager: default
        auto_generate_proxy_classes: %kernel.debug%
        mappings:
            ScribeMantleBundle: ~
            custom:
                type: yml 
                prefix: Scribe\MantleBundle\Tests\Fixtures\app\Entity
                dir: "%kernel.root_dir%/Entity/" 
                is_bundle: false
        resolve_target_entities:
            Scribe\MantleBundle\Model\AuthorInterface: Scribe\MantleBundle\Tests\Fixtures\app\Entity\Author 

security:
    encoders:
        Symfony\Component\Security\Core\User\User: plaintext

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: [ROLE_USER, ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

    providers:
        in_memory:
            memory:
                users:
                    user:  { password: userpass, roles: [ 'ROLE_USER' ] }
                    admin: { password: adminpass, roles: [ 'ROLE_ADMIN' ] }

    firewalls:
        dev:
            pattern:  ^/(_(profiler|wdt)|css|images|js)/
            security: false

        login:
            security: false

        secured_area:
            anonymous: ~

    access_control:
        #- { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY, requires_channel: https }
        #- { path: ^/_internal/secure, roles: IS_AUTHENTICATED_ANONYMOUSLY, ip: 127.0.0.1 }
