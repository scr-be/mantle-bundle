{% if cols is not null %}
	<div class="col-md-{{ cols }}">
{% endif %}
	
	<div class="form-group">
		<div class="form-errors">{{ error|raw }}</div>
    	{% if label != null %}
    		{{ label|raw }}
    	{% endif %}
		{{ widget|raw }}
	</div>

{% if cols is not null %}
	</div>
{% endif %}