---
Scribe\MantleBundle\Doctrine\Entity\Node\NodeRevision :

    type            : entity
    options         :
        row_format  : DYNAMIC
        charset     : utf8mb4
        collate     : utf8mb4_unicode_ci
    table           : Mantle_Node_Revision
    repositoryClass : Scribe\MantleBundle\Doctrine\Repository\Node\NodeRevisionRepository

    id :

        id:
            type      : bigint
            id        : true
            options   :
                unsigned : true
            generator :
                strategy : AUTO

    fields :

        content:
            type : text

    oneToOne :

        diff :
            targetEntity : NodeRevisionDiff
            mappedBy     :     nodeRevision

        owningNode :
            targetEntity : Node
            mappedBy     : latestRevision

    manyToOne :

        node :
            targetEntity : Node
            inversedBy   : revisions
            joinColumn   :
                name            : node
                referenceColumn : id

        renderEngine :
            targetEntity : NodeRenderEngine
            inversedBy   : revisions
            joinColumn   :
                name             : nodeRenderEngine
                referencedColumn : id

    manyToMany :

        embeddedNodes :
            targetEntity : Node
            inversedBy   : containerNodeRevisions
            joinTable    :
                name        : Mantle_Node_Revision_MapTo_EmbeddedNode
                joinColumns :
                    nodeRevision:
                        referencedColumnId: id
                inverseJoinColumns :
                    node:
                        referencedColumnId: id

        embeddedAssets :
            targetEntity : Scribe\MantleBundle\Doctrine\Entity\Asset\Asset
            inversedBy   : containerNodeRevisions
            joinTable    :
                name        : Mantle_Node_Revision_MapTo_Asset
                joinColumns :
                    nodeRevision:
                        referencedColumnId: id
                inverseJoinColumns :
                    asset:
                        referencedColumnId: id

...
